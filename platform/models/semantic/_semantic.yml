models:
  - name: metricflow_time_spine
    description: >
      Contiguous daily date series (2022-01-01 to 2030-01-01) required by MetricFlow
      for time-series metric computation.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
